'use strict';

/**
 * @module devices-api/types
 */

/**
 * Defines the list of available devices' types.
 * @namespace
 */

var TYPES = {

  /**
   * VEOBOX type.
   *
   * @const
   * @type {Number}
   * @default
   * @inner
   */
  VEOBOX: 1

};

Object.freeze(TYPES);
module.exports = TYPES;
