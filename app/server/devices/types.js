'use strict';

/**
 * @module devices
 */

/**
 * Defines the list of available devices' types.
 *
 * @class TYPES
 * @static
 */

var TYPES = {

  /**
   * VEOBOX type.
   *
   * @property VEOBOX
   * @type Number
   * @final
   * @static
   */
  VEOBOX: 1

};

Object.freeze(TYPES);
module.exports = TYPES;
